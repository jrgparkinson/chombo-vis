{
  "name": "chombojs",
  "version": "0.0.1",
  "description": "Visualisation for Chombo AMR data",
  "main": "src/index.js",
  "author": "Jamie Parkinson",
  "license": "BSD",
  "dependencies": {
    "vtk.js": "^13.12.2"
  },
  "scripts": {
    "heroku-prebuild": "npm install --dev",
    "build": "webpack --progress --colors --mode development",
    "build:release": "webpack --progress --colors --mode production",
    "start": "./node_modules/webpack-dev-server/bin/webpack-dev-server.js --content-base ./dist",
    "commit": "git cz",
    "semantic-release": "semantic-release",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "json-loader": "^0.5.7",
    "kw-web-suite": "^9.0.0",
    "webpack-dev-server": "^3.10.3"
  }
}
